<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <title>Trang đăng nhập</title>
  </head>
  <body>
    <h1 class="d-flex justify-content-center mt-5">Đăng nhập</h1>
    <div class="d-flex justify-content-center mb-3">
      <span class="text-danger"
        ><% if (msg?.length) { %> <%= msg %> <% } %> </span
      ><span class="text-success"
        ><% if (logoutMsg?.length) { %> <%= logoutMsg %> <% } %> </span
      ><span class="text-success"
        ><% if (successMsg?.length) { %> <%= successMsg %> <% } %>
      </span>
    </div>
    <main class="form-signin w-50 m-auto">
      <form action="/dang-nhap" method="post">
        <div class="form-floating mb-3">
          <input
            class="form-control"
            type="email"
            name="email"
            id="floatingInput"
            placeholder="Email..."
            value="<%= req?.olds?.email %>"
          />
          <label for="floatingInput">Email...</label>
          <span class="text-danger"><%= req?.errors?.email %></span>
        </div>
        <div class="form-floating">
          <input
            class="form-control"
            type="password"
            name="password"
            id="floatingPassword"
            placeholder="Password..."
          />
          <label for="floatingPassword">Password...</label>
          <span class="text-danger"><%= req?.errors?.password %></span>
        </div>
        <div class="form-check text-start my-3">
          <input
            class="form-check-input"
            type="checkbox"
            value="remember-me"
            id="flexCheckDefault"
          />
          <label class="form-check-label" for="flexCheckDefault">
            Hãy nhớ tôi
          </label>
        </div>
        <button class="btn btn-primary w-100 py-2">Đăng nhập</button>
        <div class="d-flex justify-content-between">
          <a href="/dang-ky" class="d-flex justify-content-end mt-3"
            >Quên mật khẩu?</a
          >
          <a href="/dang-ky" class="d-flex justify-content-end mt-3"
            >Tạo tài khoản mới</a
          >
        </div>
      </form>
    </main>
  </body>
</html>
