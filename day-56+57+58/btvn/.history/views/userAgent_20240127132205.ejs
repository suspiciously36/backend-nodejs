<h1>Danh sách thiết bị đã đăng nhập:</h1>

<div>
  <% for( [index, item ] of userAgentInfo.entries() ) { %> <% if
  (item.dataValues.is_logged_in) { %> <%=console.log(index);
  console.log(item[1]); console.log(item[index]); %>
  <p>Tên thiết bị: <%= item.dataValues.device_type %></p>
  <p>Tên hệ điều hành: <%= item.dataValues.os_name %></p>
  <p>Tên trình duyệt: <%= item.dataValues.client_name %></p>
  <p>Đăng nhập lúc: <%= item.dataValues.login_time %></p>
  <p>Đăng xuất lúc: <%= %></p>
  <% if (userAgent === item.dataValues.user_agent) { %>
  <p class="text-danger">---- (Current Session) ----</p>
  <% } %>
  <form action="<%=`/dang-xuat-only` %>">
    <button class="btn btn-warning">Đăng xuất thiết bị này</button>
  </form>
  <hr />
  <% } %> <% if (!item.dataValues.is_logged_in) { %>
  <p>Tên thiết bị: <%= item.dataValues.device_type %></p>
  <p>Tên hệ điều hành: <%= item.dataValues.os_name %></p>
  <p>Tên trình duyệt: <%= item.dataValues.client_name %></p>
  <p>Lần cuối đăng nhập lúc: <%= item.dataValues.login_time %></p>
  <p>Đăng xuất lúc: <%= item.dataValues.logout_time %></p>
  <% if (userAgent === item.dataValues.user_agent) { %> <% } %>
  <button class="btn btn-warning" disabled>Thiết bị này đã đăng xuất</button>
  <hr />

  <% } %> <% } %>
</div>

<form action="/dang-xuat-all">
  <button class="btn btn-danger mb-3">Đăng xuất ở mọi thiết bị</button>
</form>
